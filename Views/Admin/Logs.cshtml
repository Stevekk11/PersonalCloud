@using Microsoft.AspNetCore.Mvc.Localization
@model PersonalCloud.Models.AdminLogsViewModel
@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Admin - Logy"];
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-10">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h2 class="mb-0"><i class="bi bi-journal-text me-2"></i>@Localizer["Systémové logy"]</h2>
                <a asp-action="Console" class="btn btn-outline-dark btn-sm">
                    <i class="bi bi-terminal me-1"></i>@Localizer["Správcovská konzole"]
                </a>
            </div>

            @if (!Model.IsAuthenticated)
            {
                <div class="card shadow-sm">
                    <div class="card-header bg-dark text-white">
                        <i class="bi bi-lock me-2"></i>@Localizer["Vyžadována autentizace administrátora"]
                    </div>
                    <div class="card-body">
                        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                        {
                            <div class="alert alert-danger" role="alert">
                                <i class="bi bi-exclamation-triangle me-2"></i>@Model.ErrorMessage
                            </div>
                        }
                        <form method="post" asp-action="Logs">
                            @Html.AntiForgeryToken()
                            <div class="mb-3">
                                <label for="Password" class="form-label fw-bold">@Localizer["Heslo administrátora"]</label>
                                <input type="password" class="form-control" id="Password" name="Password"
                                       placeholder="@Localizer["Zadejte heslo administrátora"]" autocomplete="current-password" required />
                            </div>
                            <button type="submit" class="btn btn-dark w-100">
                                <i class="bi bi-unlock me-2"></i>@Localizer["Zobrazit logy"]
                            </button>
                        </form>
                    </div>
                </div>
            }
            else
            {
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <div class="d-flex align-items-end gap-2 flex-wrap">
                        @if (Model.LogFiles.Count > 1)
                        {
                            <form method="get" asp-action="Logs" class="d-flex align-items-end gap-2 flex-wrap">
                                <div>
                                    <label for="selectedFile" class="form-label fw-bold mb-1">@Localizer["Soubor logu"]</label>
                                    <select class="form-select" id="selectedFile" name="selectedFile" onchange="this.form.submit()">
                                        @foreach (var file in Model.LogFiles)
                                        {
                                            <option value="@file" selected="@(file == Model.SelectedFile)">@file</option>
                                        }
                                    </select>
                                </div>
                            </form>
                        }
                        else
                        {
                            <span class="text-muted">
                                <i class="bi bi-file-text me-1"></i>@(Model.SelectedFile ?? Localizer["Žádné soubory logů nebyly nalezeny"].Value)
                            </span>
                        }
                    </div>
                    <form method="post" asp-action="Logout">
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-outline-secondary btn-sm">
                            <i class="bi bi-box-arrow-right me-1"></i>@Localizer["Odhlásit se"]
                        </button>
                    </form>
                </div>

                <div class="card shadow-sm">
                    <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
                        <span><i class="bi bi-terminal me-2"></i>@(Model.SelectedFile ?? Localizer["Žádný soubor"].Value)</span>
                        <small class="text-light opacity-75">@Localizer["Posledních 500 řádků"]</small>
                    </div>
                    <div class="card-body p-0">
                        <pre class="m-0 p-3" style="background:#1e1e1e; color:#d4d4d4; font-size:0.8rem; max-height:70vh; overflow-y:auto; white-space:pre-wrap; word-break:break-all;">@Model.LogContent</pre>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
